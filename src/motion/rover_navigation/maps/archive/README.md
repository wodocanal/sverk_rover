# Map archive

`ros2 run rover_navigation rover_map save <label>` moves the previous
`maps/current` directory here before activating the newly saved map.

Archive directories are source data and are intentionally not installed into
`install/`. They remain part of the project and any project ZIP backup.
