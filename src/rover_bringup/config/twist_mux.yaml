twist_mux:
  ros__parameters:
    use_stamped: false
    topics:
      navigation:
        topic: /cmd_vel_nav
        timeout: 0.5
        priority: 50
      test:
        topic: /cmd_vel_test
        timeout: 0.5
        priority: 75
      teleop:
        topic: /cmd_vel_teleop
        timeout: 0.5
        priority: 100
